# Copy this file to .env and fill in real values. .env is gitignored and never committed.

# Roboflow API key — used only by models/archive/iteration_1/dataset.py to (re)download
# the SEAMaP datasets. Not required to run or build the PolyVision application.
ROBOFLOW_API_KEY=
